{
  "name": "checklist-mover",
  "version": "0.1.0",
  "description": "When a todo is checked it moves to the bottom; option to hide completed.",
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/main.js --bundle --platform=node --target=es2019 --outfile=main.js --sourcemap --external:obsidian",
    "clean": "rm -f main.js"
  },
  "devDependencies": {
    "esbuild": "^0.18.17"
  },
  "author": "IsaiahVan",
  "license": "MIT"
}
